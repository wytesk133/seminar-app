extends layouts/user

block content
  // audio(controls)
  //  source(src=say_path+'?t='+random, type="audio/wav")
  a(href=event_page_path)
    img(src=images_path+'/logo.png', style='max-width:100%;max-height:100%')
  h2.text-center Tap the image to sign in
