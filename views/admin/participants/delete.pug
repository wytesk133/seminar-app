extends ../../layout

block content
  form(method='post')
    p Are you sure you want to delete #[strong= participant.name]?
    button(type='submit') Yes
    a(href=participant_path(participant)) No
