extends ../../layouts/admin

block content
  form(method='post')
    h2 Are you sure you want to delete #[strong= user.full_name]?
    p
      button.btn.btn-danger(type='submit') Yes
      | &nbsp;
      a.btn.btn-default(href=user_path(user)) No
