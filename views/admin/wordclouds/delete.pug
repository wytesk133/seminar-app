extends ../../layouts/admin

block content
  form(method='post')
    h2 Are you sure you want to delete #[strong= wordcloud.question]?
    p
      button.btn.btn-danger(type='submit') Yes
      | &nbsp;
      a.btn.btn-default(href=wordcloud_path(wordcloud)) No
